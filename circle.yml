deployment:
  prod:
    branch: master
    commands:
      - bundle exec jekyll build
      - aws s3 sync _public/ s3://bagel-blog/ --delete
      - for i in `find . -type d -print | cut -c 3-`; do aws s3api put-object --bucket bagel-blog --key $i  --website-redirect-location "$i/" done
